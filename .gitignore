# Env files — never commit secrets
.env
.env.local
backend/.env
frontend/.env.local

# Foundry build artifacts
contracts/out/
contracts/cache/

# Foundry broadcast logs — local anvil (31337) and dry-runs are noise;
# real-network runs (Sepolia at M3) stay committable as the deploy record
contracts/broadcast/*/31337/
contracts/broadcast/**/dry-run/

# Rust build artifacts
backend/target/

# Next.js build artifacts
frontend/.next/
frontend/node_modules/

# OS
.DS_Store
Thumbs.db

# Local working files — never push
plan.md
PLAN.md
CONCEPT.md
.claude/
