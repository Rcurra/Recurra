# Env files — never commit secrets
.env
.env.local
backend/.env
frontend/.env.local

# Foundry build artifacts
contracts/out/
contracts/cache/

# Foundry broadcast logs — raw per-tx JSON, redundant with deployments.md's
# curated addresses/blocks/tx-hashes. Noise either way (anvil, dry-run, or
# real network); the deploy record lives in deployments.md instead.
contracts/broadcast/
contracts/broadcast/**/run-latest.json

# Rust build artifacts
backend/target/

# Next.js build artifacts
frontend/.next/
frontend/node_modules/

# OS
.DS_Store
Thumbs.db

# Local working files — never push
plan.md
PLAN.md
CONCEPT.md
.claude/
