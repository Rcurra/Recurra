export default function ActivityPage() {
  return (
    <div className="mx-auto flex max-w-3xl flex-col items-center px-6 py-24 text-center">
      <p className="numeric mb-3 text-[11px] uppercase tracking-[0.24em] text-ink-faint">Activity</p>
      <p className="text-sm text-ink-muted">Receipts arrive with payment history — F5.</p>
      <p className="mt-1 text-xs text-ink-faint">Every charge will land here the moment it clears.</p>
    </div>
  );
}
