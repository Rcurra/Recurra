// Wallet feature barrel — the door out of Recurra's custody world:
// balance, the full receive address, and Send (guarded, irreversible,
// never delegated to any session key — see lib/wallet.ts transferUsdc).
export { WalletModal } from './WalletModal';
